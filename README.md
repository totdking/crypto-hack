# crypto-hack

### To spin up a venv for python package isolation
1. `python -m venv venv`
2. `source venv/bin/activate`
3. deactivate : `deactivate`
4. add a .gitignore and put the venv files in it to avoid committing to git